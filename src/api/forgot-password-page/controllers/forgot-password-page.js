'use strict';

/**
 * forgot-password-page controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::forgot-password-page.forgot-password-page');
